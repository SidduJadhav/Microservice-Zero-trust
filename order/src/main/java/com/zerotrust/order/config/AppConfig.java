package com.zerotrust.order.config;

import org.springframework.context.annotation.Configuration;

/**
 * Placeholder for future beans (mTLS SSLContext, tracing, etc.)
 * Phase 3 will add SslWebClientConfig here to swap in the mTLS WebClient.
 */
@Configuration
public class AppConfig {
    // intentionally empty — extended in later phases
}