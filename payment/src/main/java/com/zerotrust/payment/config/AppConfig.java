package com.zerotrust.payment.config;

import org.springframework.context.annotation.Configuration;

/**
 * Placeholder — Phase 3 adds mTLS SSLContext here
 * to enforce mutual TLS on the embedded Tomcat server.
 */
@Configuration
public class AppConfig {
    // intentionally empty
}